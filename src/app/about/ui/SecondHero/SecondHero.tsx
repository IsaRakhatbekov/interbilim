import styles from "./SecondHero.module.scss";
export const SecondHero = () => {
  return (
    <section className={styles.aboutHero}>
      <div className={`${styles.aboutHeroContainer} container`}></div>
    </section>
  );
};
